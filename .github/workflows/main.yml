name: Run Docker Compose

on:
  push:
    branches:
      - main
jobs:
  docker:
    timeout-minutes: 1
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    # - name: Get directory
    #   run: ls
        # name: Login to Docker Hub
        # uses: docker/login-action@v3
        # with:
        #   username: ${{ secrets.DOCKERHUB_USERNAME }}
        #   password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Login to Docker Hub
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login docker.pkg.github.com -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: build container
      run: docker build -t test .

    - name: tag_it
      run: docker tag my-image ${{ secrets.DOCKER_USERNAME }}/test

    - name: push container
      run: docker push ${{ secrets.DOCKER_USERNAME }}/test

    - name: clean up
      run: docker -rmi test
      run: docker -rmi ${{ secrets.DOCKER_USERNAME }}/test


        # name: Build and push
        # uses: docker/build-push-action@v5
        # with:
        #   push: true
        #   tags: user/app:latest

    # - name: Stop containers
    #   if: always()
    #   run: docker-compose -f "docker-compose.yaml" down
